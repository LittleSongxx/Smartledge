# 设计权威（指针）

根级不维护长篇设计系统。生产前端 `vue/` 以这三处为准：

1. `vue/src/design-system/DesignSystemLab.vue` — 组件与 Token 实验室
2. `vue/src/assets/tailwind.css` — CSS Token
3. `scripts/check-f03.mjs` — F03 门禁

不要修改 `vue-claude/`。
